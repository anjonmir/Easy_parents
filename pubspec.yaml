name: easy_parents
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.10.0 # Ensure this version or a compatible one
  firebase_auth: ^4.7.0 # Ensure this version or a compatible one
  cloud_firestore: ^4.8.0 # Ensure this version or a compatible one
  url_launcher: ^6.1.6 # Latest stable version
  http: ^1.1.0 # Latest stable version
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
